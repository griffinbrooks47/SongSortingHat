

/* Format for rendering a user profile. */
import { User } from "@/types/user";



export default async function User({
        params,
    }: {
        params: Promise<{ artist: string }>
    }) {
    return (
        <main>
            
        </main>
    )
}