/**
 * Genres
 */
model DBGenre {
  /**
   * Identifiers
   */
  id String @id @default(uuid())

  /**
   * Metadata
   */
  name String @unique

  /**
   * Artists associated with this genre
   */
  artists DBArtist[]
}
